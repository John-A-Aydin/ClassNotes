
### Linear

A system is linear if it has these two properties:

##### Scaling Property
	$x(t) \rightarrow y(t)$
	then
	$Cx(t)\rightarrow Cy(t)$

##### Additive Property
	$x(t)\rightarrow y(t)$
	then
	$x(t-T)\rightarrow y(t-T)$

